@model  IEnumerable<SnilsList21.Models.Snils>

@{
    ViewData["Title"] = "Главная";
}

        <div>
            <a class="abutton" asp-controller="Snils" asp-action="Create">Создать новую задачу</a>
            @foreach (var item in Model)
            {
                <p>@item.Number</p>
            }
        </div>
